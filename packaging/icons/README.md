# Icons

Platform icons are optional and intentionally not faked.

Place real app icons here when available:

```text
app.icns  macOS jpackage icon
app.ico   Windows jpackage icon
app.png   Linux jpackage icon
```

If an icon file is missing, packaging scripts continue without a custom icon.
